# Rolecraft 设计文档

Rolecraft 是一套把个人工作方式写成文件、并编译到多个 AI 平台的框架。

---
---

## 一、目的与背景

### 1.1 背景

开发者在长期工作中会形成稳定的判断标准：什么算做完，哪些代价可以接受，什么样的结果算合格。
但是，当他把工作交给AI，试图提高工作效率时，问题却出现了——AI总是没法按照他的意愿行动，端到端来看，效率不升反降。

> 比如AI在修复代码bug时，倾向以最短路径达成目标，以至于写出让底层引用上层概念的代码。虽然功能可用，但是原有分层被破坏。

> 又比如，开发者希望与AI进行多轮对话与评审来完成一份文档的设计。但AI倾向于将设计文档视为对话的延申，将对话内容胶水般地贴附上去。这份文档慢慢变得不知所云，逻辑混乱。

这并不是因为 AI 能力不足。它能理解分层，也能写出结构完整的文档，知道什么内容适合放进文档。
究其本质，**只要一个行为能让对话继续推进，AI就可能采用，无论它是否适合这项工作**。

### 1.2 现有做法的局限

#### 1.2.1 逐次指正

逐次指正是最直接的做法：开发者主动检视AI的行动，在合适的时机告诉AI，它的行动错了，以及为什么错。但这种方式是治标不治本的：
- **指正不改变判断方式。** 一次指正处理的是当次的那一个行为。AI 理解了这条指令，但它的判断方式没有变化，同类问题会以另一种形式再次出现。相同性质的错误需要反复指正。
- **指正无法指向想法。** 人无法直接查看 AI 的想法，也就无法指出是哪一步判断出了问题，只能等它的结果出现在眼前。

> 比如在AI用破坏架构的方式修复bug后，开发者向其解释clean code是什么，遵循架构能带来什么好处，当前被破坏的架构如何修复。
> 在AI被多次鞭策后，确实看起来变得更懂架构了。但这是不可持续的——只要重新开启一个对话，这些内容都会烟消云散。哪怕不这么做，多轮compact也会消减AI对于架构的注意力。

#### 1.2.2. 通过harness engineering管理

harness engineering是解决上述问题最有效的方式。
但问题在于，人想让 AI 按某种方式工作，得自己写 skill、配 hook、调编排。这要求开发者同时懂“我想让 AI 怎么做”和“这个平台怎么扩展”。
rolecraft的独特之处在于，它是应用侧的，只关心何时该做什么，而把skill、subagent等harness概念视为执行层。
开发者并不直接编写执行层，而是让llm自己把应用侧的语言编译成执行层的语言。

---

### 1.3 Rolecraft的解决方案

Rolecraft 提供一个抽象层，让人和 AI 用同一套概念描述工作应该怎么做。
抽象的单位是角色。一个角色是一组文件，按三层组织：

- **场景层**：在什么情境下需要处理。场景描述工作当下的样子，只回答“现在是不是这种情况”，不展开做法。
- **行动层**：这种情况下的做法，以及完成之后如何确认质量。
- **认知层**：为什么这样做。它是角色对问题的理解，供行动引用，也用于没有现成做法可依的新问题。

三层分开，是为了分层披露。场景层只做辨识，内容短，可以整体保留在上下文里；行动与认知在场景命中后才展开，因此可以写得充分，而不必把全部细节一次性塞进上下文。
三层也是三种不同的东西：场景描述“何时”，行动描述“做什么”，认知描述“凭什么”。一个场景可以指向一份行动，一份行动可以引用多份认知，因此各层用同一套标识互相配对与引用。

> 在这个抽象层下，人和 AI 的沟通方式发生变化：
> - 人写下场景与对应行动，而不是写下对某次输出的修改要求。
> - AI 先辨识场景，再读取该场景对应的行动，按其中内容工作。
> - 出现偏差时，双方讨论的是哪个场景辨识不准，或哪份行动不完整，或哪条认知不成立。修改后在下一次及同类情形生效。
> - 人和 AI 读写的是同一份文件。人可以查看场景，判断 AI 何时会采取行动；也可以查看行动与认知，判断它会如何行动。这是“想法不同”能够被讨论的前提：想法以文件形式存在，双方都能看到。

**Rolecraft 不规定场景、行动与认知如何编写。**

框架只读取少量结构性的约定：三层各占一个文件夹，层内以子文件夹名作为标识，场景与行动按同名子文件夹配对。除此之外，三层各自写什么、怎么写、放几份，全部由角色编写者决定。
框架给出的是建议，不是约束。例如建议认知层按 memory、knowledge、methodology 三层组织，建议场景文件夹以同名文件作入口。这些建议可以整体不采纳，角色照样成立。
抽象层不限定工作类型。场景按情境划分，不按领域划分。出现一类此前没有涉及的工作时，补充场景与对应行动即可，抽象层本身不需要改变。
也正因为Rolecraft是抽象化的，落地到具体的harness平台上时，需要**经过编译，把三层固化到平台**，例如skill/agent/插件等。

---
---

## 二、Rolecraft的概念

### 2.1 角色的成分

一个角色由一份自述和三层内容组成，其文件结构如下所示。
```
readme.md                  角色的职责，角色可以做什么，必需
scenarios/<id>/            场景：什么情况下需要处理，必需，至少一个
actions/<id>/              行动：这种情况下的做法，必需，与场景一一对应
cognitions/<id>/           认知：为什么这样做，可选
```

以标识 `arch.layer-direction` 为例，一个完整的问题在三层里的分布是：
```
scenarios/arch.layer-direction/     什么情况下需要处理
actions/arch.layer-direction/       怎么做
cognitions/...                      为什么这样做，由行动的路由设计引用
```

场景与行动都是必需的。场景负责辨识，认知负责依据，两者都不执行；执行发生在行动里。一个场景匹配上而没有行动可执行，等于识别出了情况却无事可做。
认知是可选的。有行动而无认知的角色成立，此时行动里的做法没有写明依据。编写时可以先把场景与行动写出来，认知留到需要时再补。

以上三层描述的是"这个角色做什么"。角色还要在具体的平台上跑起来，因此另有两项与三层并列的组成：

```
readme.md                  对外说明，人和 AI 判断这个角色是否与自己有关
adapt/                     这个角色在各平台上如何落地，供编译时读取
```

---

### 2.2 自述

自述回答：**这个角色是做什么的，它要减少哪些情况。**
角色被读取时，人和 AI 最先看到的是这份自述。人据此判断这个角色与自己的工作有没有关系；AI 据此判断当前任务是否属于它的适用范围。
自述不描述角色的内部构成。角色由哪些场景、哪几份认知组成，看目录即可。自述描述的是这个角色针对的问题。

#### 什么样的自述是好的

列出反复出现的情况，让读到的人认出自己遇到过哪些。
```markdown
# committer

在改动进入提交之前评审它。

## 提交代码时反复出现的几种情况
- 功能跑通了，分层被改掉了，当时看不出来，改动多了才发现
- 为了拿到上层的一个数据，让底层直接引用上层的概念
- 上层越过中间件直接调用底层接口，换底层实现时要改一片调用点
- 直到有人指出"这是依赖倒置"，才发现上一轮拦截拦错了

我要减少的是这几种情况。
```

好的自述有三个特征。
**列的是问题状态。** “读的人却问'所以这是要做什么'”是问题状态，读者能回忆起这种情况；“交付前通读一遍”是处理动作，读者看到的是这个角色的做法。前者让人认出自己，后者介绍的是角色本身。
**不涉及内部构成。** “我的判断来自内建的原则”这类句子对外界没有意义——读的人不关心判断存在哪里。角色由哪些部分组成，看目录即可。
**不作无限承诺。** 结尾是"我要减少的是这几种情况"，一个角色减少这些情况的程度取决于它覆盖的多寡。写成"我能解决这些问题"会把范围说死。

#### 自述与场景的关系

两者都在说"什么情况下用得上这个角色"，区别在于观察的位置。

场景是角色自己判断用的：它描述当前工作的样子，由角色在运行时反复比对。

自述是给外部看的：它描述这类工作反复出现的问题，由人和 AI 在选用这个角色时读一次。

同一件事在两处出现，写法不同。场景写"我准备把当前改动提交进 git 仓库"，自述写"功能跑通了，分层被改掉了，当时看不出来"。前者是当下的辨识条件，后者是长期的困扰。

---

### 2.3 场景

场景回答一个问题：**现在是不是需要处理的情况。**
角色在工作时首先辨识场景，辨识之后才读取对应的行动。场景因此是入口：它决定角色在什么时候动用哪一套做法。
这对场景提出两点要求。
- 其一，它要能在工作当下被认出来，因此描述的是当前工作的样子，而不是需要预测或回顾才能判断的东西。
- 其二，它要能与别的场景区分开，否则命中的是哪一个并不确定。

> 场景只负责辨识，做法写在行动里。一条场景写得越像做法，越容易在修改做法时被迫一起改，而它的辨识作用并不会因此变强。

#### 场景会重叠

两个场景同时命中是常态，无法避免。“我要提交改动”与“改动让下层引用了上层”会在同一时刻都成立，因为后者正是前者的一部分。
需要分辨的不是重叠本身，而是重叠的两类。
一类是**粒度的差别**：一个场景界定整个任务，另一个只界定任务中的某一步。“我要提交改动”是任务，“改动让下层引用了上层”是其中一步的判断。二者不在同一个层面上，同时命中不构成歧义。
另一类是**并列的争用**：两个场景界定的是同一粒度的不同情况，而它们可能同时成立。“改动让下层引用了上层”与“改动让上层绕过了中间件”可以同时成立，此时该按哪一条走没有答案。

场景清单只收录前一类中的粗粒度场景。细粒度的判断放在行动内部，由行动在执行过程中辨识，不作为入口参与匹配。这样既避免了并列争用，也不要求框架引入优先级——清单里的每个场景都是独立入口，不存在先后。

**“粗粒度”的界线无法在写下场景时确定。**
写下一个场景时，作者知道它描述一件他能认出来的任务，但无法预知某个判断日后会不会变成另一个场景的内部步骤。“改动让下层引用了上层”最初很可能是作为一个独立场景写下的——它确实是一件需要处理的事。只有当它与另一条场景总是同时命中时，粒度问题才暴露出来。
因此一条判据：**若两个场景总是同时命中，它们要么应当合并，要么其中一个是另一个的内部判断。**
这是事后的判据，不是事前的要求。场景清单在使用中收敛，不是一次写对。分裂出来的细粒度判断移入行动，合并掉的场景从清单里消失，都是正常的编辑动作。

#### 什么样的场景是好的

| 写法 | 问题 |
|---|---|
| 我要修改代码 | 范围过大，几乎所有工作都会命中，等于没有入口 |
| 我要保证代码质量 | 描述的是愿望，不是可辨识的当下情况 |
| 我要按分层原则处理依赖 | 描述的是做法，属于行动 |

好的场景有三个特征：在工作当下可辨认，与其他场景的粒度不同，不描述做法。
- 在工作当下可辨认，指写的是此刻能看到的情况，不需要预测，也不需要事后回顾。“这次改动会不会引发问题”不是场景，“这次改动跨越了两个模块”是。
- 与其他场景的粒度不同，指清单里的场景各自界定一个任务，而不是同一任务中的不同判断。同一粒度的两个场景同时成立时，该走哪一条没有答案。
- 不描述做法，指场景只说明何时需要处理。一旦写进做法，改做法就要改场景，而辨识作用没有变强。

---

### 2.4 行动

行动回答：**这种情况下的做法是什么，做完之后如何确认质量**。
如果行动本身比较简单，可以直接在`actions/<id>/`下的文档中讲解清楚。
对于行动比较复杂，例如需要多种认知混合工作的场景，此时行动的内容就是行动内部的分派策略，说明如何把它拆成多种子情况、每种情况适配什么认知、结果如何汇总，等等。

配对是标识的配对，不是文件的配对。一份行动是一个文件夹，内部放几份文件由编写者决定。一件由多个环节组成的事——“检查改动”与“违规时重构”——是同一个文件夹里的两份文件，不是两份行动。

场景与行动之间是一对一：一个场景对应一个 `actions/<id>/`。这带来的是限制，也是好处，它使“这个场景会触发什么”有唯一答案。若一件事确实与多个场景相关，做法上应当把它写成多个场景各自引用的认知，而不是让多份行动互相调用。

#### 什么样的行动是好的

**做法与完成条件是分开的两件事。** 做法说明执行顺序，完成条件说明做完之后看什么来确认。两者混在一起时，质量确认会随做法的细节一起被改动。
**完成条件可以回答是或否。** “依赖方向与改动前一致”可以回答，“保证代码质量”不能。
**路由说明适用情形，不重复做法。** 路由的作用是分派，把不同情形的差别指向不同的认知。若它在重复做法，那就没有分派。
**不包含场景。** 行动里再写一遍“什么情况下适用”，会与场景产生第二份描述，两者迟早不一致。

---

### 2.5 认知

认知回答：**为什么这样做。**
行动给的是做法，认知给的是做法背后的理解：哪些做法被验证过、它们之间有什么关系、在什么条件下不成立。做法会变，理解变得慢一些；遇到没有现成做法的新问题时，可依据的也是这一层。

#### 什么样的认知是好的

框架建议认知层内部按三层组织。这不是读取约定，而是一种常见的积累方向：
- **memory**：经历过的事情，不要求像日志一样每条都记录，而是记录AI评估有意义的，值得留档的内容。这是最容易变化的一层。
- **knowledge**：从多条经历中看出的重复，是有适用范围的说法。它比 memory 更抽象，因此更不容易变化。
- **methodology**：组织知识的方式，用于面对没有现成知识的问题。它是最慢变化的一层。

---

### 2.6 适配

适配层回答：**这个角色在各个平台上如何落地。**

角色是一组文本，它本身不会运行。要让它生效，需要按目标平台的能力把它编译成那里的组件。角色写到什么程度、编译之后要达到什么效果，由编写者决定，AI 只负责实现。这一层就是编写者向编译过程提出的要求。

编写者对编译流程负责。一份 rolecraft 要能落地，除了三层写清楚，还要说明它在每个平台上要满足什么。这与代码需要配套构建配置是同一件事：C 源码本身不能运行，编译参数由作者给出，构建结果由作者验收。

```
adapt/
    opencode.md                编译到 opencode 时的提示词
    claude-code.md             编译到 Claude Code 时的提示词
```

一个平台一份文件，文件名即平台。

**适配文件的读者是编译器，不是人。** 它写给执行编译的 AI 读，因此不解释自己的写法，不交代它为什么这样写。那些是属于编写者思考过程的内容。

**给的是要求和质量标准，不是实现方式。** 说明要满足什么、要看到什么，不指定产出哪些文件、每个文件叫什么、用哪个机制。怎么实现由 AI 按平台的能力决定。

**约束用要求的形式表达。** “不希望为了用它而改变自己的工作方式”比“不要写得太复杂”更容易被正确执行，也不容易把实现方式限死。

**要求编译器报告落地结果。** 每个部分靠什么触发、落在哪里、哪些要求在这个平台上实现不了、实现不了会有什么影响。AI 报出它做不到的部分，比它自行绕过更有用。

#### 什么样的适配是好的

以下是一份适配文件的全文，编译到 opencode：

```
docwriter/ 是我写的一份 rolecraft，描述我希望文档在被我读到之前经过
什么样的检查。

请把它编译成这个项目的 opencode 组件。

需要注意的几点：

- 这个角色要在正常工作过程中起作用。我不希望为了用它而改变自己的工作方式，
  也不希望它在无关的时候打扰我。
- 它有两件事要做：文档写完准备交付时检查一遍；文档被退回之后复盘，把这次的
  错误记下来，以后不再犯。
- 认知部分会随着使用增长，它在运行中要能读到、也能写进去。

我要看到的输出是这样的：

- 一套组件，装好之后我照常工作，该触发的时候它自己触发
- 一份说明，写清楚每件事情靠什么触发、对应的内容落在哪里、认知部分怎么被读到
- 一份说明，讲清楚这个平台上哪些要求实现不了，以及实现不了的部分会有什么影响

我自己会去读 docwriter/ 里的文件，所以你不必把它们的内容贴回给我。
文件的组织方式要让我能直接找到、直接改。
```

它只出现了一次平台名，没有出现该平台的任何机制名词，也没有规定产出的文件。

**适配层是可选的。** 没有它，编译器按通用方式处理；有它，编译结果更贴近编写者的预期。

---
---

## 三、一份优秀实践

`docwriter` 是一个写设计文档与说明文档的角色。全文见 `examples/docwriter/`。
本章分三段：它为什么被编写，它如何被编译，它编译后如何发挥作用。

### 3.1 为什么编写这个角色

AI 写出的文档反复出现同一类问题。功能描述是对的，内容也在，但读的人读完一圈说不清结论是什么；行文把对话里的东西带了进去，逻辑断在半途；同一件事前半篇一个叫法，后半篇另一个叫法。这些问题不是内容错误，是行文错误，因此不能靠补充说明解决。

开发者把这些情况写成了一个角色：

```
docwriter/
    readme.md
    adapt/                                      适配到各 harness 的提示词
    scenarios/
        before-delivery/                        文档写完准备交付
        self-reflection/                        文档被退回，且退的是风格而非内容
    actions/
        before-delivery/
            how-to-do.md                        交付前的检查
        self-reflection/
            how-to-do.md                        复盘，把错误归档进 cognitions
    cognitions/
        knowledge/
            writing-style.md                    文风与 markdown 排版倾向
            bad-smell/                          五类常见错误，逐条对照
        methodology/
            reader-awareness.md                 先确定读者是谁、他要得到什么
            top-to-down-thinking.md             按抽象层次组织文档
            write-it-clean.md                   单个句子怎么写
```

`readme.md` 说明它是做什么的：

```markdown
LLM写的设计文档或说明文档，总会反复出现下面几种情况
- 通篇看完一圈，行文逻辑混乱，观点不内聚，说不清结论是什么
- 把文档当成对话的延申，让文档的逻辑断裂
- 同一件事，前半篇叫一个名字，后半篇叫另一个

本角色要减少的就是这几种情况。
```

自述不涉及角色内部的构成。它列的是这个角色要减少的几种情况，让读到它的人和 AI 判断这个角色是否与自己的工作有关。角色由哪些场景、哪几份认知组成，看目录即可。

两个场景各自界定一件任务。`before-delivery` 对应文档写完准备交付的时刻；`self-reflection` 对应文档被退回、且退回来的意见针对风格而非内容的时刻。后者是一条学习通路：它只在职责范围内出错时才触发，内容上的错误由别的方式处理。

`cognitions/` 下有两类内容，都在角色写下时即存在。

`knowledge/bad-smell/` 是五类常见错误，每类一份文件：

```
illogical-flow       逻辑散乱
dialogue-residue     对话残留
term-drift           用语漂移
unsupported-claim    断言无据
useless-meta         无用元内容
```

`methodology/` 是三条方法，`writing-style.md` 是文风与排版上的选择。它们的分工写在文件里：方法论管读者是谁、内容如何组织，风格管采用什么表述与什么排版。

`actions/before-delivery/how-to-do.md` 写明了它按什么路径去读这些认知：

```markdown
step1. 发现问题
1. 查阅`cognitions/knowledge/bad-smell`下的知识，了解在撰写文档过程中有哪些常见错误，然后逐条对照排查。

step2. 解决问题
1. 使用`cognitions/methodology/reader-awareness`明确读者是谁、他读完要得到什么，据此判断哪些内容该留、哪些该删。
2. 使用`cognitions/methodology/top-to-down-thinking`方法论梳理文档的行文思路，给出章节/子章节标题设计。
3. 使用`cognitions/knowledge/write-it-clean`知识，对有问题的章节做重构。
```

行动文本进入产物后原样保留，因此读取发生在执行到这一步的时候。这也是认知不必参与编译的原因：行动记录的是路径，不是内容。
`examples/docwriter/adapt/opencode.md` 里存着编写者对这个角色落地到 opencode 的要求，其写法见 §2.6。
角色至此写完。

### 3.2 如何编译这个角色

开发者把它交给 AI，要求编译成 opencode 的项目组件。`examples/docwriter/adapt/opencode.md` 里存着这段提示词：

```
docwriter/ 是我写的一份 rolecraft，描述我希望文档在被我读到之前经过
什么样的检查。

请把它编译成这个项目的 opencode 组件。

需要注意的几点：

- 这个角色要在正常工作过程中起作用。我不希望为了用它而改变自己的工作方式，
  也不希望它在无关的时候打扰我。
- 它有两件事要做：文档写完准备交付时检查一遍；文档被退回之后复盘，把这次的
  错误记下来，以后不再犯。
- 认知部分会随着使用增长，它在运行中要能读到、也能写进去。

我要看到的输出是这样的：

- 一套组件，装好之后我照常工作，该触发的时候它自己触发
- 一份说明，写清楚每件事情靠什么触发、对应的内容落在哪里、认知部分怎么被读到
- 一份说明，讲清楚这个平台上哪些要求实现不了，以及实现不了的部分会有什么影响

我自己会去读 docwriter/ 里的文件，所以你不必把它们的内容贴回给我。
文件的组织方式要让我能直接找到、直接改。
```

这段提示词里没有出现任何 opencode 的概念，也没有指定产出哪些文件、每个文件叫什么。开发者说的是他需要什么、要看到什么，怎么实现由 AI 按这个平台的能力决定。
约束以要求的形式给出，不以禁令的形式给出。“不希望为了用它而改变自己的工作方式”比“不要写得太复杂”更容易被正确执行，也不容易把实现方式限死。
编译产出以下文件：

```
opencode.json                                注入指令
AGENTS.md                                    角色身份，以及认知目录的位置
.opencode/skills/docwriter-check/SKILL.md    交付前的检查与复盘流程
docwriter/                                   原样保留，运行时可读写
```

编译报告如下：

```
场景               触发方式                        落地文件
before-delivery    注入指令识别"文档写完了"         AGENTS.md
self-reflection    注入指令识别"文档被退回"         AGENTS.md

行动               落地位置
before-delivery    skills/docwriter-check/SKILL.md
self-reflection    skills/docwriter-check/SKILL.md

认知               落地位置
knowledge/         docwriter/cognitions/，运行时读取
methodology/       同上

降级
- 场景辨识不由机器保证，依赖模型对注入指令的理解
- opencode 没有配置文件形式的 hook，交付这个时机无法硬触发
```

三层在编译中的处理不同。
场景编译成辨识机制。行动编译成可加载的流程，文本原样保留，其中的路径因此保持不变。认知的内容不参与编译，行动对它的依赖以路径形式记在行动文本里，编译时不作解析，只在报告中列出。
这样处理有一个后果：修改一份认知不需要重新编译，改动立即生效。角色的自我进化依赖这一点。

### 3.3 编译后如何发挥作用

#### 3.3.1 交付前的检查

开发者让 AI 写一份模块说明文档。写完，AI 说文档可以了。

`before-delivery` 命中。docwriter 逐条对照 `bad-smell/` 检查，找出三处问题。

第一处是断言无据。文中有一句"这个接口的调用频率不会太高"，没有数据支撑。docwriter 追问依据，发现原本的说法来自开发者在对话中的一句估计，而文档里没有写。改法是把频率的上限写出来，说明它由什么决定。

第二处是对话残留。文中有一句"按前面讨论的改法"，读者不知道前面讨论过什么。改法是删掉这句，把它指向的改法直接写出来。

第三处是排版。全文有十余处空行夹在同一个段落的两行之间，读起来每一句都像独立的一段。改法是按 `writing-style` 里的密排规则删掉这些空行。

三处都改完，文档交给开发者。

#### 3.3.2 被退回

开发者读完，退回：这份文档读起来还是散的，每讲一件事就断一次，几件事之间看不出关系。

退的是行文，不是内容。`self-reflection` 命中。docwriter 按 `actions/self-reflection/how-to-do.md` 复盘，写下第一条 memory：

```markdown
# cognitions/memory/2026-09-20-scattered-flow.md

## 问题背景

正在撰写一份模块说明文档，介绍一个通信模块的对外接口与使用方式。
文档面向的是要接入这个模块的开发者，不是原作者。

## 犯了什么错

开发者退回的意见是：读起来还是散的，每讲一件事就断一次，几件事之间
看不出关系。

## 直接原因

我把每一段都写成了一个独立的说明，段与段之间没有递进关系。
文档的层次没有事先确定，我是按接口的先后顺序依次写下来的。

## 根本原因

我把"写全"当成了目标，没有先确定读者读完这份文档应该得到什么认识。
```

按 `learning-criteria`，同类问题此前没有出现过，不升级。docwriter 的做法没有变化。

#### 3.3.3 第二次退回

两周后，另一份文档被退回，意见是同一类：结构散，看不出各部分的关系。

`self-reflection` 再次命中。第二条 memory 落盘。到这里，同一类问题出现了两次，满足升级条件。
docwriter 从两条 memory 提炼出一条 knowledge：

```markdown
# cognitions/knowledge/bad-smell/illogical-flow.md

## 触发条件和适用边界

适用于一切需要他人阅读的文档。不适用于给自己看的草稿与随手记录。

## 典型表现

- 通篇读完，说不出这份文档想解决什么
- 章节之间是并列关系还是递进关系，看不出来
- 同一个问题在第三节提出、第七节才回答，中间夹着无关内容
- 每节单独看都成立，连起来读不出作者想说什么

## 正确做法和验证方式

先确定文档的层次，再在每层内部展开。层次是从抽象到具体的下降，
不是论证步骤的先后。

验证方式：只看各节标题，能否复述出这份文档在讲什么。做不到说明层次没定，
或者标题没有承担各自的职责。
```

这条认知要求改变做法。原有的检查只在交付前发生，而层次问题是写的时候就落下的。因此 docwriter 修改了 `actions/before-delivery/how-to-do.md`，在 step2 里把"先梳理行文思路、给出章节标题设计"提到重构之前。

从这一刻起，docwriter 处理每一份文档都会先定层次，再动笔改内容。

#### 3.3.4 第三次退回：一类只出现一次的问题

一个月后，一份文档被退回，意见是文中出现了与内容无关的东西：文档开头写了版本号和修订记录，正文里夹了两处"本次修改新增了"。

`self-reflection` 命中。docwriter 记下第三条 memory，但判定它不升级——这是一类新问题，此前只出现过一次，而且它是否属于本角色的职责还需要观察：版本号在某些文体里是发布流程要求的。

memory 留在原处，做法暂不改变。

### 3.4 这个角色达成了什么

三段之后，`examples/docwriter/` 的内容是：

```
docwriter/
    readme.md
    adapt/
        opencode.md                             适配提示词
    scenarios/
        before-delivery/
            when-to-do.md
        self-reflection/
            when-to-do.md
    actions/
        before-delivery/
            how-to-do.md                        （步骤已修改）
        self-reflection/
            how-to-do.md
    cognitions/
        knowledge/
            writing-style.md
            bad-smell/
                illogical-flow.md               （新增）
                dialogue-residue.md
                term-drift.md
                unsupported-claim.md
                useless-meta.md
        methodology/
            reader-awareness.md
            top-to-down-thinking.md
            write-it-clean.md
        memory/
            2026-09-20-scattered-flow.md        （新增）
            2026-10-04-...
            2026-11-02-...
```

同类问题出现两次的那一条升为 knowledge 并改写了行动；只出现一次的那一条留在 memory。
这个差别是 `learning-criteria` 在起作用：同一类问题出现两次以上、且能写出它在什么条件下不成立，才允许升级。第二条不满足时宁可不升，避免从一次偶发情况里总结出过宽的规律。

这一节与 3.1 的问题对照：AI 写出的文档行文散乱，而开发者此前只能逐次指出。在这个案例里，开发者只给出了退回意见，没有修改任何文件。归类、提炼、改检查步骤，都由 docwriter 自己完成，写在它自己的目录里。
开发者查看 `cognitions/knowledge/` 可以了解它现在依据什么判断，查看 `cognitions/memory/` 可以了解它经历过什么、哪些升了级、哪些没有。

### 3.5 它学不会什么

docwriter 的学习只由一种信号驱动：文档被退回，且退回的意见针对风格与行文。

它无法从内容性的错误里学习，那类问题由别的方式处理。它也无法从自己的判断里学习——一份文档没有被退回，不等于它写得好，可能只是开发者没有细看。

这一边界意味着 docwriter 的进化依赖人在场，它不是自主的，只在有人参与时前进。
